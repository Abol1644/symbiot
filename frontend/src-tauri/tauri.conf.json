{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "symbiot",
  "version": "2.0.0",
  "identifier": "io.symbiot.mission-control",
  "build": {
    "beforeDevCommand": "pnpm dev",
    "devUrl": "http://localhost:5173",
    "beforeBuildCommand": "pnpm build",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "title": "symbiot mission control",
        "width": 1440,
        "height": 900,
        "minWidth": 920,
        "minHeight": 640,
        "resizable": true
      }
    ],
    "security": {
      "csp": "default-src 'self' http://localhost:8100 ws://localhost:8100; connect-src 'self' http://localhost:8100 ws://localhost:8100; style-src 'self' 'unsafe-inline'; img-src 'self' data:"
    }
  },
  "bundle": {
    "active": true,
    "targets": ["nsis", "appimage", "deb"],
    "createUpdaterArtifacts": false,
    "shortDescription": "Mission control for a sandboxed agent factory",
    "longDescription": "symbiot turns PROJECT.md specifications into tested Docker artifacts through a human-gated multi-agent pipeline."
  },
  "plugins": {
    "updater": {
      "pubkey": "REPLACE_WITH_UPDATE_PUBLIC_KEY",
      "endpoints": [
        "https://updates.example.invalid/symbiot/{{target}}/{{arch}}/{{current_version}}"
      ]
    }
  }
}
